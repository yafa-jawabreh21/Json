{
  "name": "book-store-app-server",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --host 0.0.0.0 --port $PORT --watch data.json"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
